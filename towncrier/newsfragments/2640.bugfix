Bugfix to the sieve/vacation module:
 - uses the correct From address (instead of an SRS alias)
 - replace sieve_vacation_dont_check_recipient=no with sieve_vacation_use_original_recipient=true to prevent loops
